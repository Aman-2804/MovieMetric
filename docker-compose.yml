services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: moviegpt
      POSTGRES_PASSWORD: moviegpt
      POSTGRES_DB: moviegpt
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:

